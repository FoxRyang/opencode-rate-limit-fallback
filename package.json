{
  "name": "opencode-rate-limit-fallback",
  "version": "0.4.0",
  "description": "OpenCode plugin that automatically switches to fallback models when rate limits are hit - with chat notifications",
  "type": "module",
  "main": "index.ts",
  "files": [
    "index.ts",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "opencode",
    "rate-limit",
    "fallback",
    "model",
    "plugin",
    "multi-model"
  ],
  "author": "forked from sk2andy/liamvinberg",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/opencode-rate-limit-fallback.git"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
